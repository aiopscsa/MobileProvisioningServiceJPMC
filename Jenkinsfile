pipeline {
 agent any
 stages {
    stage('build') {
         steps{
           script {
            echo "Maven Build"
            sleep(time:28,unit:"SECONDS")
           }
         }
    }
   }
 
 }
